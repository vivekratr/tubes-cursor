## Learned User Preferences

- Keep the VECTOSOLVE logo shape and original colors; do not recolor it as disco, neon-gaming, or heavy glitch.
- Prefer premium, scientific, or optical motion (holographic scan, light refraction, DNA assembly) over arcade/glitch treatments.
- Iterate landing experiments on versioned routes (`/v1`, `/v2`) instead of replacing `/`.
- When writing public-facing copy about the current coming-soon page, do not name the `/v2` route.

## Learned Workspace Facts

- This repo is a coming-soon landing with isolated experiment pages at `/v1` and `/v2`; `/v2` holds the DNA/particle helix treatment plus a `data-theme` dark/light toggle.
- Theme preference is stored in `localStorage` as `coming-soon-theme` and defaults to dark.
- A portable React extraction of the effect lives under `React-component/`.
